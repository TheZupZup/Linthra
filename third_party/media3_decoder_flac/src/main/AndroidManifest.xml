<?xml version="1.0" encoding="utf-8"?>
<!-- Linthra's manifest for the vendored Media3 FLAC module. Upstream's declares
     the namespace through the `package` attribute, which AGP 8 rejects; the
     namespace lives in build.gradle instead. No permissions, no components. -->
<manifest />
